# Secrets — NEVER commit these
.env
token.pickle
credentials.json

# Utility scripts that output secrets — keep local only
scripts/encode_token.py

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
venv/
env/
*.egg-info/

# Database — too large for GitHub
chroma_db/
*.db
*.sqlite3

# Uploads — temporary files
uploads/
/tmp/

# Logs
logs/
*.log

# IDE
.vscode/
.idea/

# AWS deployment artifacts
.aws/
*.zip
apprunner-bundle/
elasticbeanstalk-bundle/
ecs-params.yml
samconfig.toml
.aws-sam/
cdk.out/
cdk.context.json

# Keep these deployment templates (not secrets)
# ecs-task-definition.json  <- tracked, contains no secrets
# apprunner.yaml            <- tracked
# nginx.conf                <- tracked
# AWS_DEPLOYMENT.md         <- tracked
